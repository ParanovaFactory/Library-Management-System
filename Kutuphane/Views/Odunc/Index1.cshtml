@using Kutuphane.Models.Entity
@using PagedList
@using PagedList.Mvc
@model PagedList.IPagedList<tblHareket>
@{
    ViewBag.Title = "Index1";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h1>Ödünç Kitap Listesi</h1>
<table class="table table-striped">
    <tr style="background-color:rgb(252, 178, 22)">
        <th>İşlem Id</th>
        <th>Kitap Adı</th>
        <th>Üye</th>
        <th>Personel</th>
        <th>Alış Tarihi</th>
        <th>İade Tarihi</th>
        <th>Üyenin Getiriş Tarihi</th>
    </tr>
    @foreach (var x in Model)
    {
        <tr>
            <td>@x.Id</td>
            <td>@x.tblKitap.Ad</td>
            <td>@x.tblUyeler.Ad @x.tblUyeler.Soyad</td>
            <td>@x.tblPersonel.Ad @x.tblPersonel.Soyad</td>
            <td>@Convert.ToDateTime(x.AlısTarihi).ToString("dd/MM/yyyy")</td>
            <td>@Convert.ToDateTime(x.IadeTarihi).ToString("dd/MM/yyyy")</td>
            <td>@Convert.ToDateTime(x.UyeGetirTarihi).ToString("dd/MM/yyyy")</td>
        </tr>
    }
</table>

@Html.PagedListPager((IPagedList)Model, sayfa => Url.Action("Index1", new { sayfa }))